<h1>Editing Post</h1>

<% if user_signed_in? %>
  <%= render 'form', post: @post %>
<% else %>
  <p>You need to be signed in to edit a post </p>
<% end %>

<%= link_to 'Show', @post %> |
<%= link_to 'Back', posts_path %>
