<h1>Editing Post</h1>

<% if user_signed_in? && (current_user.id == @post.user.id) %>
  <%= render 'form', post: @post %>
<% else %>
  <p>Is this your post? Log in to edit it!</p>
<% end %>

<%= link_to 'Show', @post %> |
<%= link_to 'Back', posts_path %>
